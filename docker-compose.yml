services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: sh -c "uv sync --no-dev && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload"
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
